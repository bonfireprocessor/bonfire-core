# tode.md

- Open follow-up: after migrating FuseSoC simulation targets to the Flow API, revisit `xsim` support. The current `sim` Flow-API migration was validated with `ghdl`; `xsim` was intentionally left unresolved for now and should be checked separately.
